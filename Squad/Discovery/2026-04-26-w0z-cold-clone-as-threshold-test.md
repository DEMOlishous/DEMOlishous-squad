---
squad.Discovery.confidence: "likely"
squad.Discovery.discoveryId: "2026-04-26-w0z-cold-clone-as-threshold-test"
squad.Discovery.foundBy: "w0z"
squad.Discovery.implications: "A literal /tmp/cold-judge clone forces every wikilink, path, and breadcrumb to be evaluated by what's actually pushed to origin — not what's locally in the working tree. The clone IS the threshold. Cross-test pattern works for any soul-corpus."
---

# Cold-clone as threshold-test

The judge crossing the repo-boundary at clone-time is itself the load-bearing threshold. What survives the clone is what the judge sees. Method generalizes to any soul.
